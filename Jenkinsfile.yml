pipeline:
  agent: any
  stages:
    - stage: Build
      steps:
        - echo 'Building...'
        - sh 'make build'
    - stage: Test
      steps:
        - echo 'Testing...'
        - sh 'make test'
    - stage: Deploy
      steps:
        - echo 'Deploying...'
        - sh 'make deploy'
  post:
    always:
      - echo 'This will always run'
    success:
      - echo 'This will run only if successful'
    failure:
      - echo 'This will run only if failed'
